export const LOG_CONTENT = {
    PING: "Ping count: "
}

export enum LogType {
    PING="PING",
    UNKNOW="UNKNOW",
}

export enum LogFrom {
    GUEST="GUEST",
}

export enum PaginationConstant {
    DEFAULT_PAGE=1,
    DEFAULT_LIMIT=8,
    DEFAULT_OFFSET=0
}